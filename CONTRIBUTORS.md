# Contributors

- `eric-stone-plus` - product direction, requirements, domain decisions, and
  repository ownership.
- Codex (OpenAI) - repository audit, fail-closed safety work, deterministic
  analysis core, regression coverage, workbench implementation, verification,
  and release documentation in the controlled-prototype milestone.

The initial public commits also carry a `Co-authored-by` trailer for Codex so
the machine-readable Git history and this human-readable record agree.
